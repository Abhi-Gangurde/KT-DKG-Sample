resources:
  jobs:
    sample_job:
      name: sample_job_
      email_notifications:
        on_failure: dhiraj.nskd@gmail.com
      tasks:
        - task_key: sample_notebook_job
          notebook_task:
            notebook_path: /Workspace/Users/dhiraj.nskd@gmail.com/Analysis/GeettingSampleData
            source: WORKSPACE
          environment_key: Default
      environments:
        - environment_key: Default
          spec:
            environment_version: "5"
      # new_cluster:
      #   spark_version:
      #   node_type: Standard_D4ds_v5
      #   num_worker: 1